<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="tu.paquete.aqui">

    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE"/>

    <application
        android:label="desktop"
        android:name="${applicationName}"
        android:icon="@mipmap/ic_launcher">
        <!-- Configuración de la actividad MainActivity -->
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">
            <!-- Especifica un tema de Android para aplicar a esta Activity tan pronto como
                 el proceso de Android se haya iniciado. Este tema es visible para el usuario
                 mientras la IU de Flutter se inicializa. Después de eso, este tema continúa
                 determinando el fondo de la ventana detrás de la IU de Flutter. -->
            <meta-data
              android:name="io.flutter.embedding.android.NormalTheme"
              android:resource="@style/NormalTheme"
              />
            <!-- Configuración del filtro de intentos para la actividad -->
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        <!-- No elimines la meta-data a continuación.
             Esto es utilizado por la herramienta Flutter para generar GeneratedPluginRegistrant.java -->
        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />
    </application>
    <!-- Configuración para consultar actividades que pueden procesar texto -->
    <queries>
        <intent>
            <action android:name="android.intent.action.PROCESS_TEXT"/>
            <data android:mimeType="text/plain"/>
        </intent>
    </queries>
</manifest>
